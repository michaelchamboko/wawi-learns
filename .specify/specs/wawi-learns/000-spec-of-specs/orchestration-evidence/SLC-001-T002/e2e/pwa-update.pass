task_id: SLC-001-T002
check_kind: e2e
check_id: pwa-update
result: pass
recorded_at: '2026-08-11T13:45:00Z'
command: npm run build && npm run test:e2e -- tests/e2e/spikes/pwa-update.spec.ts
test_count: 3
candidate_tree: see commit HEAD^{tree}
commit: HEAD
implementation_fingerprint: see commit HEAD^{tree}
fingerprint_resolution:
  description: The evidence file is part of the committed tree, so the committed tree hash is the implementation fingerprint. The evidence file records the commit SHA as the stable handle.
source_path: tests/e2e/spikes/pwa-update.spec.ts
evidence_owner: @Jimmy
notes:
  - Manifest endpoint serves name "Wawi Learns", display "standalone", start_url "/", icons 192x192 + 512x512 (SVG).
  - /sw.js is generated by Serwist with shell revision 'eba6ff0' embedded for / and /offline precache entries.
  - Offline navigation reopens home-shell and falls back to /offline shell when navigating to an unknown route.
  - SKIP_WAITING message handling: Serwist registered handler activates new worker only after explicit message; old worker keeps control mid-session.
  - Hosted Vercel receipt and GitHub Actions run for the matching SHA still required to satisfy AC-SLC-001-002; local evidence is a developer preview, not the production receipt.